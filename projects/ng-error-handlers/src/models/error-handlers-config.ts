export interface ErrorHandlersConfig {
    /**
     * Angular's default
     * `ErrorHandler` will be included and all error additional handlers
     * will be placed alongside with it.
     */
    preserveDefault: boolean;
}
